#!/usr/bin/env python3
"""Assemble or refresh one Azure intake without manufacturing authority."""
import pathlib
import sys

sys.dont_write_bytecode = True
sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent))
from atlas_intake import main

if __name__ == "__main__":
    raise SystemExit(main())
