# Harness Adapters

Canonical tools live in `skills/` and `agents/`. Harness directories are install targets generated by `scripts/install_harness.py`; generated files carry source, manifest version, and checksum metadata.

Use `scripts/verify_harness.py --target <path>` in target repositories to detect manual edits or stale generated copies.
