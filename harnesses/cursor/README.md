# Cursor Adapter

Installs skills to `.cursor/skills/<name>/` and agents to `.cursor/agents/`.

```bash
python3 scripts/install_harness.py --harness cursor --target /path/to/project
```

The committed `.cursor/` tree in this repo is generated from the same adapter for compatibility.
